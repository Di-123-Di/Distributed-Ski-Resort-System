pipeline:
  name: distributed-ski-resort-pipeline-validation-error
  description: Pipeline with validation errors - fails at validation stage before job creation

stages:
  - validate
  - compile

# Job with missing required image field
validation-job:
  - stage: validate
  # ERROR: Missing required 'image' field
  - script:
    - echo "This job will fail validation due to missing image field"


compile-job:
  - stage: build
  - image: maven:3.8-eclipse-temurin-8
  - script:
    - echo "This job will fail validation due to wrong stage name"
